import * as React from "react";

const IndexPage = () => {
  return (
    <main>
      <h1>Index Page</h1>
    </main>
  );
};

export default IndexPage;

export const Head = () => <title>Home Page</title>;
